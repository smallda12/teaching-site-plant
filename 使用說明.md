# 植物小百科｜教學網 使用說明

自然科　115 學年度第 1 學期　第 1 單元　第 1～4 週（115.08.31 – 115.09.25）

---

## 一、怎麼開

**教室電腦（教室版）**
直接雙擊 `index.html` 就能用，不需要安裝任何東西、不需要網路。
右上角會顯示黃色的「教室版」標籤，點名區可以上傳學生照片。

**放到網路上（公開版）**
把整個 `教學網站` 資料夾上傳到 GitHub Pages 或任何網頁空間即可。
網站會自動偵測，右上角顯示藍色「公開版」標籤，**點名區的照片上傳會自動關閉**，只留文字輸入。

> 版本是自動判斷的：本機開啟 = 教室版，網址開啟 = 公開版。
> 要強制指定，改 `js/config.js` 最上面的 `模式`，填 `"教室版"` 或 `"公開版"`。

---

## 二、六大分區

### 🙋 點點名
- 建立班級名單（姓名／照片），大按鈕抽籤點名，抽到會唸出名字。
- 抽籤結果**左邊是大照片、右邊是大姓名**，全班在後排也看得到。
- **一輪只會點到每個人一次**：抽過的人不會再被抽中，名單上會變灰並標「✓ 已點」。全班點完會顯示「這一輪結束」並停住，要再點一輪請按「↺ 重新開始一輪」。
- 進度會記住，中途重新整理網頁也不會忘記已經點過誰。

### 🃏 圖詞輪播
- 左邊大圖，右邊把**標題（詞）與文字說明拆成兩個獨立按鈕**，可以分開重複點按練習。
- 可上一張／下一張／隨機抽／自動輪播（8 秒一張）。
- 切換到新的一張時會**先唸詞（例：根），停頓一下，再唸說明句（根住土裡，會吸水）**；
  下方「🔊 完整唸一次」可重播整段。

### 🎬 教學影片
播放單元教學影片（內含字幕）。

### 📚 教材教學區
- 四個教學重點，流程是**教學頁 → 主題短片 → 3 題形成性評量**。
- **四個重點全部開放，不用照順序上**，老師可以依課堂需要直接點想教的那一個。
- 每個重點都有一支對應的主題短片（植物五個部位／水的旅行／植物繁殖／木本與草本）。
  **每一頁教學頁都有「🎬 看教學短片」按鈕**，講到一半想放短片就能直接放，不用先翻到最後一頁。
  在短片畫面可以「再看一次」，或按「回教學頁」**回到該重點的第一頁**重新看起；離開時影片會自動暫停。
- **🔊 自動朗讀開關**（在進度卡上）：關掉之後，**切換頁面不會自動出聲**，老師可以自己解說不受干擾。
  但主動按「🔊 再唸一次」仍然照樣播。設定會記住，下次打開沿用。
- 「▶ 播放開場」也套用專注版面（圖片依剩餘高度縮放、不會被切掉），播完自動進入重點 1，
  中途也可以按「← 回重點清單」離開。
- 「▶ 播放開場」放完 P01–P03 後，會**自動進入重點 1**。
- 答對時會先把「答對了，你好棒！」**完整播完**，才切換到解說畫面，語音不會疊在一起。
  （這段約 3 秒，學生會看到自己按的選項維持綠色。）
- 任何時候點新的語音按鈕，前一段語音會立刻停掉，快速連點也不會重疊。
- 進度會存起來，關掉重開仍然記得。

### 📝 評量區
- 12 題總評量＋各重點正確率統計，另可開啟學習單、練習單、圖卡、簡報 PDF。
- **答錯不重答、也不會說「再想一想」**，只把正確答案標成綠色讓學生看一眼，1.4 秒後直接進下一題
  （這裡是一次做完 12 題的總評量，不是練習；要反覆練習請用教材教學區）。
- 答對才會有「答對了，你好棒！」的鼓勵語音。

### 🧠 自學區
- **20 張**圖卡，**點一下會放大成全螢幕檢視**，圖片與文字都變大，同時唸出來（先唸詞、停頓、再唸說明句）。
- 放大後，**標題與說明各自是一顆發音按鈕**，可以分開重複點按練習；底下另有「🔊 完整唸一次」。
- 用左右箭頭（或鍵盤 ← →）換張，按右上角 ✕（或 Esc、點背景）關閉再挑別張。
- **▶ 自動輪播全部圖卡**：依序放大每一張並唸出來，**唸完才換下一張**（不是固定秒數，長句不會被切掉）。
  可勾選「**只播標題，不唸說明**」——初次認識時快速過一遍，複習時再唸完整說明。
  點圖卡、按左右箭頭、關閉放大、切到別的分區，都會自動停止輪播。

### 一個畫面裝得下，不用捲動
教學頁、題目、解說畫面會自動隱藏區塊標題與說明，圖片依剩餘空間放到最大，
**上課時不需要滾動滑鼠**。答對後直接換成解說畫面，不會往下疊加。

---

## 響應式：電腦、平板、手機都能用

同一個網址在不同裝置會自動換版面，不需要另外做手機版。

| 裝置 | 版面調整 |
|---|---|
| **大螢幕電腦**（≥1500px） | 字級再放大一級，點名照片 340px、姓名 100px |
| **一般教室電腦**（1025–1499px） | 標準版面，圖詞輪播左圖右字 |
| **平板**（601–1024px） | 導覽換行成兩排全部可見、字級略縮、圖卡三欄 |
| **手機**（≤600px） | 導覽兩排、圖詞輪播改上下排列、點名照片與姓名上下排、圖卡兩欄、工具面板貼齊左右 |
| **手機橫拿／矮螢幕**（高度≤560px） | 導覽壓成單排、字級與間距再收緊，確保選項與按鈕不被擠出畫面 |

實測結果（題目畫面，**六種尺寸全部零捲動、選項與按鈕全可見**）：

| 解析度 | 題目圖高 |
|---|---|
| 1920×1080 | 573px |
| 1366×768 | 275px |
| 1024×768（平板橫） | 218px |
| 768×1024（平板直） | 493px |
| 375×812（手機直） | 195px |
| 812×375（手機橫） | 72px |

另外處理的細節：
- 使用 `dvh` 單位，手機瀏覽器網址列出現或收起時版面不會被切掉。
- 觸控裝置取消 hover 位移效果，避免點一下後外觀卡住。
- 題目畫面用整頁 flex 撐開，導覽換行或裝置旋轉都會自動重算，不需重新整理。

---

## 三、教學重點與課程計畫的對應（62 頁版）

| 重點 | 內容 | 簡報頁 | 教學頁數 | 對應課程計畫單元內容 |
|---|---|---|---|---|
| 1️⃣ 植物的身體 | 五部位總覽＋根莖葉花果實各一頁 | P04–P16 | 7 頁 | ①認識植物構造和功能 |
| 2️⃣ 水的旅行 | 根吸水→莖送水→葉使用→正確澆水 | P17–P27 | 5 頁 | ①了解水如何進出植物的身體 |
| 3️⃣ 種出新植物 | 種子、發芽條件、插枝、果實保護種子 | P28–P38 | 5 頁 | ②實際運作根莖葉種植，觀察繁殖方式 |
| 4️⃣ 木本與草本 | 木本、草本、兩者比較 | P39–P48 | 4 頁 | ③引導學生選擇分類標準進行植物分類 |

開場 P01–P03、綜合活動與收尾 P49–P52。
學習圖卡另有獨立的 20 張（來源見下方「學習圖卡的來源」）。
每個重點的評量都是「題目頁 → 解答頁」交錯排列，共 12 題。

---

## 四、評量的選項順序（固定，與簡報一致）

**簡報本身已經把正解穿插在 A、B 兩邊**（12 題中 A 六題、B 六題），
所以網站**不再隨機打亂**——選項左右位置固定，與簡報上的 A、B 完全相同。
這樣老師用簡報講解、學生在網站上作答，看到的順序才會一樣。

| 題頁 | A 選項 | B 選項 | 正解 |
|---|---|---|---|
| P11 | 葉子 | 根 | **B** |
| P13 | 花 | 莖 | **A** |
| P15 | 果實 | 葉子 | **A** |
| P22 | 花 | 根 | **B** |
| P24 | 土裡 | 花上 | **A** |
| P26 | 長更大 | 枯掉 | **B** |
| P33 | 盒子裡 | 土裡 | **B** |
| P35 | 一段莖 | 果實 | **A** |
| P37 | 不理它 | 澆水 | **B** |
| P43 | 木本 | 草本 | **A** |
| P45 | 木本 | 草本 | **B** |
| P47 | 木本 | 草本 | **A** |

其他配套：
- 每題只需要**一個版本**的題目語音，順序必定與畫面一致。
- 題目語音**不唸「A」「B」**，直接唸兩個選項（例：「植物泡在土裡的是哪個？葉子，還是根？」）。
- 解答語音也不帶字母（例：「答案是根。根住在土裡面。」）。
- 畫面上的選項按鈕不顯示 A／B 圓形標記，只放選項文字。

> ⚠️ **簡報改版時**：如果評量頁的選項順序或正解變了，必須同步更新
> `js/data.js` 的 `選項`／`正解`，以及 `工具腳本/建置語音_全部.py` 的 `題庫`，然後重產語音。
> 兩邊不同步的話，學生會聽到跟畫面不一樣的題目。

---

## 學習圖卡的來源（容易搞混，特別註明）

學習圖卡**不是**從主簡報取的，而是另一份獨立檔案：

```
植物小百科_學習圖卡.pdf   （40 頁 = 20 張正面 + 20 張背面文字卡）
        ↓ 只取奇數頁（正面）
assets/cards/card01–card20_front.webp
```

沿革（踩過兩次）：圖卡最早取自 `圖卡_裁切版\`，中間改成主簡報的 P53–P62，
但實際上老師是在**獨立的「學習圖卡」簡報**裡改版並從 10 張擴充到 20 張的。
所以正確來源是這份 PDF。

**改圖卡時要同步三個地方**，缺一個就會出現「看到的和聽到的對不上」：
1. `植物小百科_學習圖卡.pptx` → 輸出成 `.pdf`
2. `js/data.js` 的 `圖卡` 陣列（詞、稿、類）
3. `工具腳本/建置語音_全部.py` 的 `圖卡` 清單 → 重跑語音

20 張的分組：植物的身體（1–6）、生活情境（7–10）、水的旅行（11–14）、
種出新植物（15–17）、木本與草本（18–20）。

### 📌 `圖卡_裁切版\` 不要動，也不用同步

那個資料夾是**老師自己要列印實體圖卡用的素材**，與網站無關。
建置腳本一律略過它；就算裡面的檔案更新了，網站也不需要跟著更新，
更不要建議刪除。網站的圖卡只認 `植物小百科_學習圖卡.pdf`。

---

## 五、側邊工具（全站都可用，右下角）

- 🖊️ **畫筆**：12 色可選、粗細可調、橡皮擦、一鍵清除。關掉後不會擋到操作。
- ⏱️ **計時器（三個，各自獨立）**：每個都能單獨設定分秒與「時間到要說的話」，可以同時跑不互相干擾
  （例如：1 號計全課時間、2 號計小組活動、3 號計個別練習）。
  時間到會先響「時間到」，再用語音唸出你填的那句話，該計時器的框會閃橘色。設定會自動記住。

---

## 六、語音怎麼來的

- **教材固定文字**（逐頁口播、題目、解答、圖卡、答對答錯提示）＝ 事先用 edge-tts 產好的 MP3。
  - 聲音：`zh-TW-HsiaoChenNeural`（台灣女聲）
  - 語速：`-25%`（放慢，讓學生聽得清楚）
- **老師現場輸入的文字**（學生姓名、計時器說明）＝ 用瀏覽器內建語音即時合成，同樣放慢。
  - 若唸不出來，多半是這台電腦沒裝中文語音。Windows 設定 →「時間與語言」→「語音」→ 新增中文（台灣）語音即可。

### 要換聲音或調語速
1. 編輯 `工具腳本/建置語音_全部.py` 最上面的 `VOICE` 與 `RATE`
2. 執行 `python 工具腳本/建置語音_全部.py`
3. **把 `js/config.js` 裡的 `語音.版本` 加 1**（很重要，否則瀏覽器會繼續播快取裡的舊聲音）

常用台灣聲音：
| 代碼 | 說明 |
|---|---|
| `zh-TW-HsiaoChenNeural` | 女聲，清晰活潑（目前使用） |
| `zh-TW-HsiaoYuNeural` | 女聲，溫柔柔和 |
| `zh-TW-YunJheNeural` | 男聲，穩重 |

---

## 七、學生資料放哪裡（隱私）

點名區的**姓名與照片只存在這台電腦的瀏覽器裡**（localStorage / IndexedDB），
**不會寫進任何檔案、不會上傳、也不可能被推到 GitHub。**

- 建好名單後**下次打開就直接在，不用重新上傳**。
- 想換電腦或怕瀏覽器資料被清掉，按「💾 匯出備份檔」存成一個 `.json`（含照片），
  在另一台電腦按「📂 匯入備份檔」就能還原。

---

## 八、檔案結構

```
教學網站/
├── index.html          主頁
├── 使用說明.md          本檔
├── css/style.css
├── js/
│   ├── config.js       版本設定（教室版／公開版、語音版本）
│   ├── data.js         教材資料（62 頁、12 題、20 圖卡）
│   ├── speech.js       語音
│   ├── tools.js        畫筆＋計時器
│   └── app.js          主程式
└── assets/
    ├── pages/          簡報 62 頁（WebP，7.6 MB）
    ├── cards/          學習圖卡 20 張正面（WebP）
    ├── audio/          98 支 MP3
    ├── video/          lesson.mp4、short1–4.mp4（四支主題短片）
    └── pdf/            學習單、評量練習單、圖卡、完整簡報
```

整站 198 檔、約 80 MB，最大單檔 22.4 MB（完整簡報 PDF），符合 GitHub 單檔 100 MB 限制。

---

## 九、改教材內容

- 改題目、口播文字、圖卡文字 → 編輯 `js/data.js`
- 若改到「要唸出來的文字」→ 同步更新 `工具腳本/建置語音_全部.py` 裡的對應文字並重跑，再把 `js/config.js` 的 `語音.版本` 加 1
- 改完程式或樣式 → 把 `index.html` 最底下 5 個 `?v=` 的數字加 1，否則瀏覽器會沿用舊快取
- **換掉 PDF、影片、簡報頁圖等 assets 檔案** → 把 `js/config.js` 的 `資源版本` 加 1。
  這些檔名不會變（`p11.webp`、`assessment.pdf`…），不加版本號的話瀏覽器會繼續用快取裡的舊檔——
  **換了簡報卻看到舊的評量頁、換了 PDF 卻是舊內容，都是這個原因。**

### 簡報改版的完整流程

**素材的方向要先弄清楚**（這裡曾經誤判過）：

```
pages/p01–p62.png（老師一張一張生成的原始圖，這才是來源）
        ├──→ 組成 植物小百科.pptx / .pdf
        └──→ 建置素材_圖片壓縮.py → assets/pages/*.webp（網站用）
```

所以改版流程是：

1. 換掉或新增 `pages/pXX.png`（原始圖）
2. 用這些圖更新 `植物小百科.pptx`，並輸出 `植物小百科.pdf`
3. 執行 `工具腳本/建置素材_圖片壓縮.py`（PNG → WebP，並複製影片與 PDF）
4. 若評量頁的選項或正解有變 → 更新 `js/data.js` 與 `建置語音_全部.py` 的題庫，重跑語音
5. 把 `資源版本`（有改語音的話再加 `語音.版本`）加 1，`index.html` 的 `?v=` 也加 1

> ⚠️ `工具腳本/輸出簡報頁圖.py` 是**反方向**（PDF → 覆蓋 pages）。
> 平常不要用，它會用畫質較差的重繪版蓋掉原始圖；只有原始 PNG 遺失時才拿它救援。
